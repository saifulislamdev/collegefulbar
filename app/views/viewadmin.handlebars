<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">

    <link rel="stylesheet" href="/assets/css/style.css">
    <title>Admin</title>
</head>
<header>
    <nav>
        <h4>Collegefulbar</h4>
        <ul>
            <li><a href="/add"></a>Add Course</li>
            <li><a href="/remove"></a>Remove Course</li>
            <li><a href="/viewadmin"></a>View Courses</li>
            <li><a href="/update"></a>Update Course</li>
            <li><a href="/viewStudents"></a>View Students</li>
            <li><a href="/addStudent"></a>Add Students</li>
            <li><a href="/delStudent"></a>Delete Students</li>
            <li><a href="/upStudent"></a>Update Students</li>
            <form method="post">
                <button formaction="/logout" class="pure-button button-small">logout</button></center>
            </form>
        </ul>
    </nav>
</header>

<body>
    <section id="main" class="wrapper">
        <div id="view" class="container">
            <h2>ALL COURSES</h2>
            <table>
                <tbody>
                    <tr>
                        <th> Course Name </th>
                        <th> Section </th>
                        <th> Instructor </th>
                        <th> Room </th>
                        <th> Seats </th>
                        <th> Credits </th>
                        <th> Cost </th>
                    </tr>
                    {{#each items}}
                    <tr>

                        <td>{{this.name}}</td>
                        <td>{{this.section}}</td>
                        <td>{{this.instructor}}</td>
                        <td>{{this.room}}</td>
                        <td>{{this.seats}}</td>
                        <td>{{this.credits}}</td>
                        <td>{{this.cost}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        <div class="container">
        </div>
    </section>
</body>

</html>